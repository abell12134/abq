# Python 3.10；安装：python3 -m venv quant-venv && quant-venv/bin/pip install -r requirements.txt
pyqlib==0.9.7
mlflow<3            # mlflow 3.x 文件存储后端与 qlib recorder 不兼容
lightgbm==4.6.0
baostock==0.9.2
backtrader==1.9.78.123   # 阶段2 验证层：事件驱动复演引擎
openai>=1.40             # 阶段3 因子提议：OpenAI 兼容客户端（deepseek 等）
cursor-sdk>=0.1.0        # 实盘舆情硬伤筛：Cursor Agent 联网检索（Python≥3.10）
akshare>=1.14            # 舆情记忆：财联社/新浪 7x24；TA 否决公告/新闻
pandas==2.3.3
numpy==2.2.6
pyyaml
fastapi>=0.110           # 阶段5 看板服务
uvicorn>=0.29            # ASGI 服务器
jinja2>=3.1              # 看板模板
apscheduler>=3.10        # 后台常驻定时（每日 evening/postclose/复盘）
minio>=7.2               # MinIO/S3 数据同步（服务器 push / 本地 pull）
