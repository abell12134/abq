# A 股 AI 量化交易系统 — 设计实现方案

> 目标：基于 qlib / rd-agent / backtrader / vnpy / abu 五个开源项目分层集成，
> 构建一个 **可实盘、可自动化、可持续迭代** 的 A 股量化交易系统。
> 本方案以"真实收益"为唯一导向：所有设计决策服务于把可实现的超额收益拿到手，
> 并系统性地消灭回测与实盘之间的偏差。

---

## 1. 收益从哪里来（先想清楚再动手)

实盘收益 = **阿尔法 − 交易成本 − 执行偏差 − 错误损失**。四项分别由系统的不同层负责：

| 收益项 | 负责模块 | 手段 |
|---|---|---|
| 阿尔法（超额收益） | Qlib 研究层 + RD-Agent 迭代层 | 多因子选股 + 机器学习打分，AI 持续挖掘新因子对抗因子衰减 |
| 交易成本 | 验证层 + 执行层 | 佣金/印花税/滑点全量建模；降低换手率；限价单执行 |
| 执行偏差 | backtrader 验证层 | 涨跌停、停牌、T+1、整手约束在回测中强制建模，杜绝"纸面成交" |
| 错误损失 | 风控 + 监控运维 | 自动化消灭手工失误；硬风控防止单日灾难性回撤 |

### 策略主线选择

A 股可行的系统化策略中，**指数增强型多因子选股** 是散户/小私募资金量级下
风险收益比最优、容量最大、与本技术栈最匹配的主线：

- 持仓 30~100 只股票，**低换手的再平衡**（每日可触发，但强制最小持有约 10 个交易日），
  单票权重上限 5%；（阶段1实证：字面"日频全量换仓"会被成本吃光，详见 §9.2）；
- 收益结构 = 市场贝塔（基准如中证500/中证1000） + 选股超额；
- 现实预期：**年化超额 5%~15%**（相对基准），回撤与基准相近或略低；
- T+1 制度下高频/日内策略对个人通道基本不可行，不做主线。

辅线（第二阶段后可加）：基于 Qlib 信号强度的仓位择时（极端低分时段降仓），
以及 abu 式 UMP 裁判模型对个股信号的二次否决。

### 不做什么（同样重要）

- 不做日内 T+0、不做高频（通道、成本、制度都不支持）；
- 不依赖单一"圣杯因子"，靠因子库的宽度和持续迭代；
- 不让 AI 无人值守地把新因子直接推上实盘（必须过样本外验证 + 纸面跟踪期）。

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│ L5 监控运维层（自研，约 1k 行）                                │
│    调度器(APScheduler/cron) · 告警(钉钉/飞书/邮件) · 日报      │
│    净值/持仓/偏差监控 · 异常熔断                               │
├─────────────────────────────────────────────────────────────┤
│ L4 执行层 = vn.py                                            │
│    券商网关(miniQMT/华鑫奇点/XTP) · OMS · 实时硬风控          │
│    算法执行(限价追单/TWAP) · 成交回报落库                      │
├──────────────── 数据契约：目标持仓 → 订单 → 回报 ─────────────┤
│ L3 验证层 = backtrader + 自研 UMP                            │
│    事件驱动复演(T+1/涨跌停/停牌/整手/费税) · 滑点敏感性        │
│    UMP 裁判模型(借鉴 abu)对信号二次否决                        │
├──────────────── 数据契约：每日信号 score → 目标权重 ──────────┤
│ L2 研究层 = Qlib                                             │
│    因子库(Alpha158 起步) · 模型训练(LGBM 基线) · 滚动再训练    │
│    组合优化(TopK + 权重约束) · 向量化回测                      │
├──────────────── 数据契约：因子/模型代码 + 评估报告 ───────────┤
│ L1 AI 迭代层 = RD-Agent                                      │
│    因子挖掘循环(fin_factor) · 模型进化循环(fin_model)          │
│    候选因子池 → 自动评估 → 人工评审 → 入库                     │
├─────────────────────────────────────────────────────────────┤
│ L0 数据层 = Qlib bin 格式（唯一真相源）                        │
│    akshare/baostock 日线增量 · PIT 财务数据 · 历史成分股       │
│    vn.py 实时行情(仅实盘用) · 所有数据每日校验                 │
└─────────────────────────────────────────────────────────────┘
```

集成原则：**层间只通过文件/数据库的数据契约通信，零代码级耦合**。
任何一层可独立替换、升级、回滚。abu 不作为运行时依赖，只移植 UMP 思想。

---

## 3. 模块设计

### 3.1 数据层（L0）

- **日线行情**：主路径用 [investment_data](https://github.com/chenditc/investment_data)
  社区数据包的每日 release（已是 Qlib bin、含历史时点成分股），校验后原子替换本地数据；
  baostock 逐股抓取仅作小标的池的故障退路。
  （原"akshare/baostock 每日全量抓取 → dump_bin"在当前机器不可行，见 §9.2；
  接入实盘后再用 vn.py 实时行情补当日增量。）全 A 股票 + 指数 + 成分股权重。
- **防偏差三件套**：
  1. 历史时点成分股（instruments 文件带起止日期，防幸存者偏差）；
  2. PIT 财务数据（用公告日而非报告期，防前视偏差）；
  3. 后复权价格回测、真实价格下单。
- **数据质检**：每日抓取后自动跑校验脚本，股票数量、涨跌幅分布、停牌名单
  与交易所公告核对，校验失败则告警并阻断后续流水线（宁可不交易，不可用错数据交易）。

### 3.2 研究层（L2，Qlib）

- 基线：Alpha158 因子 + LightGBM，中证500 成分内选股，TopK=50、n_drop=3、
  **最小持有 10 日**（低换手）。标签用 **5 日开盘价收益**（1 日标签信号衰减快、换手高，
  净超额被成本吃光；实测 1 日标签毛超额 +6.7% / 净 −1.4%）。
- **特征扩展**：`research/alpha158_plus_lab.py`（`Alpha158PlusLab`）在 Alpha158 之上追加
  `factor_lab` 中 `status=live` 的表达式；无 live 因子时行为与 Alpha158 完全一致，
  保证旧模型可继续推理。
- **滚动训练**（已落地）：`research/rolling_retrain.py` 每月 walk-forward 切窗重训；
  候选先写入隔离 experiment，仅当样本外 IR ≥0.8 且不低于线上 IR−tol 时，才把模型写入
  生产 experiment `baseline_alpha158_lgbm`（`predict_daily` 始终加载该 experiment 最新 recorder）。
- 训练/验证/测试严格按时间切分；所有实验配置入 git，结果入 MLflow/Qlib recorder，可复现。
- 组合构建：score 排序 → TopK-Dropout → 约束（单票 ≤5%；**行业偏离 ≤3%**，见执行层，
  仅当 `topk ≥ industry_min_positions`（默认 20）时启用，避免小资金浓缩组合被约束误杀）。

### 3.3 AI 迭代层（L1，RD-Agent）

- 跑 `fin_factor` 因子挖掘循环：LLM 提出假设 → 写因子代码 → Qlib 回测 → 根据结果迭代
  （落地为自研 `factor_lab/`，见 §9.4）。
- **因子准入流程（防过拟合的生命线）**：
  1. 候选池：RD-Agent 产出 + IC/ICIR/换手率自动初筛；
  2. 去重：与现有因子库相关性 >0.7 者剔除；
  3. 样本外：最近 2 年滚动 walk-forward 验证；
  4. 人工评审：逻辑可解释性检查（讲不出经济学逻辑的高 IC 因子默认可疑）；
  5. 纸面跟踪：入库后先跟踪 1~3 个月，线上 IC 达标才参与实盘模型
     ——已由 `factor_lab/track_paper.py` 落地：评近期 Rank IC / 同号 / |IC| 门禁；
     默认只出报告，`--promote` 才写入 `status=live`。
- 频率：每周末跑一轮迭代 + 纸面跟踪评估（`crontab.example`）；每月初滚动重训。
- **并入实盘模型路径**：`paper_tracking` →（`--promote`）`live` → `Alpha158PlusLab` 特征
  → `rolling_retrain` / `run_baseline` 过 IR 门禁 → `predict_daily` 加载新模型。
  **禁止**跳过纸面期或未重训就改线上特征维数。

### 3.4 验证层（L3，backtrader + UMP）

- **二次验证**：Qlib 信号导出 CSV → backtrader 事件驱动逐日复演，强制建模：
  - T+1（当日买入不可卖）；涨停不可买、跌停不可卖；停牌剔除；
  - 整手（100 股）取整；佣金（万 2.5，最低 5 元）+ 卖出印花税 0.05% + 滑点（基准 0.1%，并测 0.2%/0.3% 敏感性）。
- **验收规则**：backtrader 复演年化收益与 Qlib 回测差异 >3 个百分点，
  或滑点敏感性测试下超额收益归零 → 策略打回，不得上实盘。
- **UMP 裁判**（借鉴 abu，自研约 500 行）：用历史交易的特征（市场状态、个股波动、信号分位）
  训练分类器，对每日买入信号做最后否决，目标是砍掉胜率最差的尾部交易。✅ 阶段2b 已落地。

### 3.5 执行层（L4，vn.py）

#### 通道方案（已确认：现有账户为普通券商账户，前端用同花顺）

同花顺只是交易前端软件，账户实际开在券商；能否自动化下单取决于券商是否提供
程序化通道，与同花顺无关。三条路线如下，**已确定按"路线一起步、路线三为终态"执行**：

| 路线 | 方式 | 定位 |
|---|---|---|
| 一（起步，已选定） | 系统每日 17:30 生成调仓清单推送手机，次日开盘在同花顺手动下单（约 5 分钟/日）。下单以外的数据、选股、风控、对账全自动 | 阶段 1~4 的执行形态，策略验证期最稳妥 |
| 二（不采用为长期方案） | easytrader 模拟键鼠驱动同花顺 PC 客户端 | 仅 Windows、客户端升级易失灵、无可靠成交回报，跑真钱风险大，最多作过渡 |
| 三（终态） | 开通 miniQMT/QMT 程序化权限（先问现有券商；不支持则到国金/国盛等新开户转资金），`vnpy_xt` 接入 vn.py，全自动闭环 | 仿真盘验收通过后切换 |

- 行动项：阶段 1~3 期间不动账户，按路线一运行；同期向券商咨询 miniQMT 权限
  （有资金门槛，几万到几十万不等，各券商差异大）；仿真盘验收通过后切换路线三。
- 备选通道：华鑫奇点 TORA（`vnpy_tora`）、XTP（中泰，有资金门槛）。

#### 执行算法与风控

- **执行算法**（路线三）：开盘后 9:35~10:00 分批限价执行（避开集合竞价噪声），
  限价 = 对手价 ±1 tick，30 秒未成交则追单，3 次未成交放弃并记录。
- **路线一已落地的硬风控**（不依赖 vn.py）：
  - 单票权重上限（`strategy.max_weight`，研究线 5% / 小资金实盘可放宽）；
  - 涨跌停/停牌预检；UMP 买入否决；
  - **日亏硬熔断**：`risk.daily_loss_halt`（默认 3%）——`compute_nav` 触发后写
    `account.json.halt_active`，剥除当日订单中的 BUY；次日 `make_trade_plan` 禁买只卖，
    出单后自动解除。`monitor` 校验熔断中不得残留 BUY；
  - **行业偏离约束**：`data/meta/industry_map.csv`（baostock 申万一级，
    `build_industry_map.py` 生成）+ `make_trade_plan` 跳过会加剧偏离的买入候选；
    配置 `industry_deviation_max: 0.03`，仅当 `topk ≥ industry_min_positions`（默认 20）启用；
  - 实际持仓与目标持仓偏离 >2% 告警人工核查（`reconcile`）。
- **实时硬风控（vn.py RiskManager，阶段5）**：单笔/总仓上限、撤单率、日亏熔断与路线一对齐后接入通道层。

### 3.6 监控运维层（L5，自研）

- 调度：APScheduler 统一编排（见 §4 时间线），任一环节失败即告警 + 阻断下游。
- 对账：每日收盘后券商成交回报 vs 本地订单记录自动对账，不一致告警。
- 日报：净值、当日盈亏、持仓明细、换手、与回测预期的跟踪偏差，推送到手机。
- 偏差监控：实盘滑点统计 vs 回测假设，月度回顾，超标则修正回测参数。

---

## 4. 全自动每日时间线

| 时间 | 任务 | 失败处理 |
|---|---|---|
| 16:00 | 抓取当日行情 + 停复牌/分红送配公告 | 重试 3 次，仍失败则告警，次日不交易 |
| 16:30 | 数据质检 → 更新 Qlib bin 数据 | 质检不过则阻断 + 告警 |
| 17:00 | 模型推理 → 生成次日信号 score | — |
| 17:10 | UMP 否决 → 组合优化 → 生成目标持仓文件 | — |
| 17:20 | 风控预检（涨跌停预排除、停牌剔除、偏离检查） | 异常项自动剔除并记录 |
| 17:30 | 推送次日调仓计划到手机（人工可一键叫停） | 默认放行 |
| 次日 9:30 | vn.py 登录、对账、确认持仓基准 | 对不上则不交易 + 告警 |
| 9:35~10:00 | 分批限价执行调仓单 | 未成交单记录，午后 13:05 补一轮 |
| 15:05 | 成交回报落库、与券商对账、计算当日净值 | 不一致告警 |
| 15:30 | 日报推送 | — |
| 每周末 | 因子迭代一轮 + `track_paper` 纸面 IC 评估；策略周度复盘 | — |
| 每月初 | `rolling_retrain` 滚动重训 → 新旧模型样本外对比 → 达标才切换 | 不达标沿用旧模型 |

人工介入点只有两个：**17:30 的调仓计划确认（可设为默认放行）** 和
**每月因子/模型评审**。其余全自动。

> **路线一（手动执行）期间的差异**：次日 9:30~10:00 的三行由人工替代——
> 按 17:30 推送的调仓清单在同花顺手动下单（约 5 分钟），收盘后将成交记录
> 导出/录入系统完成对账与净值计算。其余环节不变。切换到 miniQMT（路线三）
> 后恢复表中全自动流程。

---

## 5. 数据契约（层间接口定义）

```
signals/YYYY-MM-DD.csv          # L2 → L3：instrument, score, rank
target_position/YYYY-MM-DD.csv  # L3 → L4：instrument, shares, last_price, entry_date
orders/YYYY-MM-DD.csv           # L4 产出：instrument, side, shares, ref_price（→ 订单号/状态）
fills/YYYY-MM-DD.csv            # L4 产出：成交回报
nav/holdings.csv                # 当前持仓：instrument, shares, last_price, entry_date
                                #   entry_date 用于 hold_thresh 计持有期（阶段1新增）
nav/daily.csv                   # L5 产出：日期, 净值, 仓位, 当日换手
nav/account.json                # 现金账本 + halt_active / halt_since（日亏硬熔断）
meta/industry_map.csv           # instrument, industry[, name]（行业约束；gitignore 运行时生成）
```

全部为带 schema 校验的 CSV（后续可平滑迁移到 SQLite/PostgreSQL），
每个文件写入后生成 `.done` 标记，下游以标记为触发条件，天然支持断点重跑。

---

## 6. 实施计划（5 个阶段，每阶段有量化验收门槛）

### 阶段 1：数据底座 + 研究基线（2~3 周）✅ 已完成
- 交付：A 股日线自动更新管道；Alpha158+LGBM 基线跑通；近 5 年回测报告。
- **验收**：数据连续 10 个交易日自动更新无人工干预（管道已就绪，连续运行待实盘期累计）；
  基线策略样本外信息比率（相对中证500）≥ 0.8 → **实际净超额 IR 1.04 达标**（前提是低换手，见 §9.2）。

### 阶段 2：验证闭环（2 周）✅ 已完成
- 交付：backtrader 复演引擎（含全部 A 股约束）；成本/滑点敏感性报告；UMP 裁判模型。
- **验收**：复演与 Qlib 回测年化差异 ≤3pct → **实际 −2.3pct 达标**；
  0.2% 滑点下年化超额仍 ≥5% → **实际 5.65% 达标**。
  附加：UMP 样本外 A/B 使超额 IR 0.21→0.48（砍尾部交易、降风险）。

### 阶段 3：AI 迭代闭环（2~3 周）✅ 已完成（纸面跟踪/并入路径 2026-07 补齐）
- 交付：LLM 因子挖掘闭环跑通（`factor_lab/`，借鉴 RD-Agent fin_factor 思想自研轻量实现，
  deepseek-v4-pro 提议）；因子准入流程（初筛/去重/样本外/评审/纸面跟踪）落地；
  因子库 `factors.yaml` 状态机管理。
- **验收已达成**：完成 3 轮迭代、累计提议 15 个候选；`pv_corr`、`delta_pv_corr`
  两个量价相关因子通过全部五道关卡，且叠加"现有因子库等权组合"后样本外 Rank IC
  增量 +0.0021 / +0.0031（≥1 个新因子过全部关卡并提升组合样本外 IC，达标）。
  反馈闭环有效：第 2 轮在第 1 轮成功因子（量价相关）上自发改良出"量价增量相关"版本。
  报告见 `data/reports/factor_iter_*.md`。
- **2026-07 补齐**：`track_paper.py`（纸面 IC 评估 / `--promote`）；
  `Alpha158PlusLab` + `workflow_baseline.yaml` 接入；`rolling_retrain.py` 月度门禁重训。
  截至 2026-07-13：两因子仍为 `paper_tracking`（纸面约 27 日 < 40 日门槛），
  近期 Rank IC 与入库 OOS 同号（负向、|IC|≈0.025），未晋升 live——符合关卡纪律。

### 阶段 4：仿真/手动小资金实盘（4~8 周，与阶段 3 并行）
- 形态：按 §3.5 路线一执行——系统全自动产出每日调仓清单，人工在同花顺下单，
  收盘后成交记录回填系统对账；可同时用现有账户小资金真实跟单。
- 当前采用双线记录：
  - **研究模拟线** `research_sim_100k`：启动资金 100000 元，系统自动记录每日模拟操作，
    按次日开盘价模拟成交，收盘后记录净值、费用、换手与持仓。
  - **实盘线** `live_manual_10k`：真实资金 **12000** 元（2026-06-22 起 `mode: manual`），
    目标持仓 5-10 只；系统生成订单，同花顺人工下单后回填 `fills`，收盘对账/净值/日报。
  两条线共享行情/信号/UMP/风控预检，但账户数据隔离在 `data/accounts/<account>/`，
  用于比较真实执行相对研究模拟的偏差。
- 交付（工具链已落地）：
  - 编排 `ops/run_daily.py`（evening：更新数据→信号→调仓清单→健康检查；
    postclose：健康检查→对账→算净值→出日报），任一步失败告警并中止下游；
  - 成交回填 `execution/record_fills.py`（模板/应用，维护持仓与现金账本）；
  - 对账 `execution/reconcile.py`（订单vs成交、目标vs实际持仓、滑点）；
  - 净值/日报/监控 `ops/compute_nav.py`+`daily_report.py`+`monitor.py`
    （含**日亏硬熔断**、跟踪偏差）；
  - 调仓清单 `make_trade_plan.py`：UMP 否决 + 涨跌停/停牌预检 + 熔断禁买
    + 行业偏离（topk 足够大时）+ 小资金残余现金二次部署；
  - 行业映射 `execution/build_industry_map.py` → `data/meta/industry_map.csv`；
  - 研究线模拟成交 `execution/simulate_fills.py`（按次日开盘价自动写 fills 并计费）；
  - 上游停更回退 `data_pipeline/update_incremental.py`（baostock 增量，含账户持仓标的）；
  - 双线复盘 `ops/review_accounts.py`（收益/超额/持仓数/现金闲置/费用/成交偏差/告警）；
  - 数据契约 `contracts/schemas.py` 统一校验（补齐 §9.3 缺口）。
- **实盘快照（2026-07-16，同花顺对账）**：
  净值 **11,651.39**（现金 4,183.39 + 市值 7,468.00），持仓 8 只，累计收益 **-2.91%**，
  相对中证500累计超额约 **+10.5%**；07-15/16 调仓订单均未成交（买不满/未持仓卖单）。
- **验收（待连续实跑累计）**：连续 20 个交易日流水线无故障；实际净值与回测预期跟踪偏差
  年化 ≤2pct；对账零差错。工具与指标框架已就绪
  （截至 2026-07-16 实盘约 **16** 个交易日，距 ≥20 日门槛还差约 4 日）。
- 同期行动：（按用户要求暂不申请 miniQMT，维持路线一手动下单）。

### 阶段 5a：后台常驻看板 + 定时（🛠 已落地，双线运行中）
- 形态：单个 FastAPI 服务（`webapp/server.py`，默认 `0.0.0.0:8000`）同时承担可视化看板
  与内置 APScheduler 定时（工作日 22:30 evening / 23:30 postclose / 周五双线复盘），
  无需 cron；`webapp/serve.sh` 负责常驻启停。
- 看板：双线净值/累计收益/超额对比、各账户持仓·成交·换手·现金·报告、双线对比（收益差/滑点偏差）、
  告警与调度面板。`ops/backfill.py` 离线重放最近 ~1 周交易日，使看板启动即有数据。
- 运行口径：`live_manual_10k` 已切 `mode: manual`（真实人工回填）；`research_sim_100k` 保持
  `simulated` 自动成交，用于度量执行偏差。
- 配套修订：`research/predict_range.py` 一次特征计算批量产出区间信号（单日逐次约 4 分钟→区间一次）；
  `predict_daily` 显式指定 mlflow `exp_manager` uri，使任意工作目录都能加载模型；
  撤掉 evening 阶段的回填模板（成交日应为次日执行日，订单日写模板会与上一日真实成交撞名）；
  `run_daily` evening 在 `update_daily` 后刷新交易日；断档用 `predict_range` + `backfill` 补跑。

### 阶段 5：全自动实盘（持续）
- 前提：阶段 4 验收通过，且 miniQMT 通道开通。
- 交付：`vnpy_xt` 接入实盘通道；下单环节切换全自动；硬风控全量启用。
- 节奏：首期投入计划资金的 10%~20%，跑满 1 个月且跟踪偏差达标后逐步加仓。
- **验收**：实盘滑点 ≤ 回测假设；月度跟踪偏差在预期范围；熔断/告警机制经过至少一次演练。

总计约 3 个月到实盘，前 4 个阶段可压缩并行。

---

## 7. 关键风险与应对

| 风险 | 应对 |
|---|---|
| AI 挖因子过拟合（最大风险） | §3.3 五道准入关卡；新因子必须过纸面跟踪期 |
| 因子衰减、超额收益变薄 | RD-Agent 持续迭代补充因子宽度；月度监控因子线上 IC |
| 回测实盘偏差 | backtrader 强约束复演 + 仿真盘 20 日验收 + 实盘滑点月度回顾 |
| 极端行情（千股跌停式） | 单日亏损熔断；跌停无法卖出是制度性风险，靠分散持仓(≥30 只)和行业约束缓释 |
| 程序 bug 造成错单 | 撤单率/订单数硬限制；对账机制；调仓计划人工可叫停 |
| 通道风险（接口变更/断线） | 开盘前对账不过则不交易；vn.py 断线重连 + 告警 |
| 依赖项目停止维护 | backtrader 冻结但稳定，仅用其回测核心；abu 不引入运行时依赖 |

---

## 8. 技术栈与目录结构

```
project/
├── qlib/  rd-agent/  backtrader/  vnpy/  abu/   # 上游项目（只读引用）
└── quant/                                        # 自有系统（本方案交付物）
    ├── data_pipeline/    # L0：抓取、质检、dump_bin
    ├── research/         # L2：qlib 工作流配置、滚动训练脚本
    ├── factor_lab/       # L1：rd-agent 配置、因子准入流程、因子库
    ├── validation/       # L3：backtrader 复演引擎、UMP 裁判
    ├── execution/        # L4：vnpy 策略、信号→订单转换、执行算法、风控
    ├── ops/              # L5：调度、监控、对账、日报、双线复盘、回填、告警
    ├── webapp/           # L5：常驻看板服务（FastAPI + APScheduler 定时）
    ├── contracts/        # 数据契约 schema 与读写工具
    └── configs/          # 全局配置 + accounts/<account>.yaml 账户 profile
```

环境：Python 3.10（qlib/rd-agent/vnpy 兼容交集）；研究与实盘分离部署
（实盘机只装 execution + ops，最小依赖）；全部配置与代码入 git。

---

## 9. 实施回顾与方案修订（阶段 1–2 已落地，依实测回填）

> 本节根据 `quant/` 阶段 1、2 的真实实现，修订前文中被实证细化或推翻的假设；
> 不改变总体架构与分层契约，只校正落地细节与暴露的风险点。

### 9.1 已验证成立的设计

- **收益分解成立、成本是头号变量**：基线信号有真实毛超额（IR 0.6~0.8），
  但每日全量调仓时净超额一度为 **−0.17**——交易成本几乎吃光全部 alpha。
  §1 把"交易成本/执行偏差"列为一等公民是正确的，且其权重比直觉更高。
- **年化超额 5%~15% 的预期得到验证**：qlib 回测净超额 9.3%，backtrader 独立复演 5.7~7%。
- **强约束复演的价值被证实**：backtrader 复演比 qlib 保守约 2.3pct（独立账户记账 +
  整手现金拖累 + 显式滑点），正是"杜绝纸面成交"的意义所在。

### 9.2 重要修订（落地后校正）

1. **主线"日频调仓"需限定为"低换手再平衡"**：必须用 5 日标签 + 最小持有约 10 个交易日
   （`hold_thresh=10`、`topk=50`、`n_drop=3`），净超额 IR 才从 −0.17 升至 1.04。
   换手率是阶段 1 的生命线，研究/验证/执行三层调仓逻辑须严格对齐（均采用
   TopkDropout + hold_thresh），否则实盘会把超额重新交还给成本。
2. **数据源主路径改为社区数据包**：本机到 baostock 链路延迟过高（登录 >3 分钟），
   6000+ 全 A 逐股抓取不可行。主路径改用 investment_data 每日 release 的 Qlib bin 包
   （已含历史时点成分股），baostock 降级为小标的池故障退路。§3.1 已同步修订。
3. **回测/实盘口径一致性是硬要求**：复演中曾误用"开盘记净值 vs 基准收盘"，导致跟踪误差
   虚增约 4 倍、超额 IR 从 0.85 跌到 0.16。净值必须收盘对收盘统一口径——这是验证层
   自身的"防偏差"纪律，应纳入回测规范。

### 9.3 仍待补齐（不影响阶段 1–2 验收）

- ~~**数据契约 schema 校验工具**（§5/§8 的 `contracts/`）~~ ✅ 已在阶段4补齐
  （`contracts/schemas.py`：列/类型/取值校验 + 带 `.done` 的读写，跨层 CSV 统一经此读写）。
- ~~**滚动再训练**（§3.2）~~ ✅ 2026-07 已落地 `research/rolling_retrain.py`
  （walk-forward + IR 门禁；crontab 每月初启用）。首次生产晋升仍须实跑一次并过门禁。
- ~~**纸面跟踪 → live → 并入特征**~~ ✅ 2026-07 已落地 `track_paper.py` + `Alpha158PlusLab`；
  当前无 live 因子，线上模型特征维数未变。
- ~~**日亏硬熔断 / 行业偏离**~~ ✅ 2026-07 已落地（见 §9.7）；行业约束对小资金实盘默认关闭。
- **数据"连续 10 日自动更新"验收**：管道与 cron/看板定时已就绪，连续无人工干预的实跑
  需在阶段 4 累计验证。
- **阶段 4 连续 20 日验收**：实盘线样本仍不足（见双线复盘报告）。
- **仓位择时辅线**（§1）：极端低分时段降仓——仍为第二阶段后可选项，未实现。
- **行业约束在 Backtrader 复演中对齐**：执行层已过滤；验证层复演尚未镜像同一规则
  （研究线 topk=50 启用时，应用复演交叉验证，优先度低于阶段4验收）。

### 9.4 阶段 3 落地校正

1. **L1 用"自研轻量闭环"替代直接跑 RD-Agent 全框架**：RD-Agent 的 `fin_factor`
   依赖其自有数据/运行时、较重且 LLM 调用不可控。改为只移植其"LLM 提假设→代码化→
   Qlib 回测→反馈迭代"的核心思想，在 `factor_lab/` 自研（`llm_propose` + `evaluate`
   + `run_iteration`），与已有 Qlib 数据/口径无缝对接，且准入关卡完全可控可复现。
2. **"提升组合 IR"的口径要对准因子库组合，而非 LGBM 基线**：最初用"原始因子 z 叠加到
   LGBM 打分上"衡量增量，结果几乎恒为负——因为 Alpha158+LGBM 已吸收线性信号。修正为
   衡量对"现有因子库等权组合"的样本外 Rank IC 增量，才正确反映新因子的边际价值。
3. **准入关卡是主交付物，因子生成是输入**：单因子要同时满足 |IC|/ICIR/换手/样本外/
   去重/增量 是高门槛，15 个候选仅 2 个通过，漏斗有效拦截了弱因子与重复因子——
   这正是 §7"AI 挖因子过拟合"头号风险的防线。
4. **LLM 凭证管理**：`configs/secret.env`（gitignore）存 key，推理模型需较大 `max_tokens`
   且需容忍 JSON 截断（解析做了逐对象抢救 + 重试）。

### 9.5 阶段 4 落地校正（路线一手动下单）

1. **"成交回填"必须以实际成交为准，而非假设按计划成交**：早期流程用 `holdings_next.csv`
   直接覆盖持仓（默认全额按参考价成交）。实盘人工下单会有未成交/部分成交/价格偏差，
   因此改为 `record_fills.py` 录入**实际成交**驱动持仓与现金账本，`reconcile.py` 再把
   计划与实际对账——这才是"对账零差错"验收的真正含义。
2. **净值口径沿用阶段2 的"收盘对收盘"纪律，且建仓首日超额计 0**：入场当日按收盘价建仓、
   未承担持有期敞口，若把当日基准全日收益计入超额会失真（与 §9.2 的 NAV 时序问题同源）。
3. **现金账本是净值的真相源**：用 `nav/account.json` 维护现金，买入扣、卖出加（含费用），
   `compute_nav.py` 用"现金 + 持仓盯市"算净值；费率口径与回测/验证层完全一致（`ops/common.py` 单一出口）。
4. **风控预检前移到出清单环节**：涨停/停牌不进买单、跌停/停牌不进卖单，避免推送无法执行的指令；
   UMP 否决也在此环节接入，使实盘选股与阶段2b A/B 验证口径一致。
5. **跟踪偏差需累计样本**：年化跟踪偏差 ≤2pct 的验收需 ≥20 个交易日才有统计意义，
   日报已实现该指标框架，dry-run 阶段如实标注"样本不足"。

### 9.6 阶段 5a 落地校正（后台常驻看板 + 定时）

1. **常驻服务 = 看板 + 调度合一**：用单个 FastAPI 进程内置 APScheduler（工作日 evening/postclose、
   周五复盘），逻辑仍是调用经验证的 `ops/run_daily.py`，服务进程只做编排与展示；
   cron 退为备选，二者只启用其一以免重复执行。
2. **evening 不应预写回填模板**：成交日期应为"次日执行日"而非订单日；在订单日写
   `fills/<订单日>.csv` 会与上一日 postclose 的真实成交文件撞名并被清空（表现为成交额/费用归零）。
   已撤掉 evening 模板步骤——研究线由 `simulate_fills` 在 postclose 产出成交，实盘线在执行日
   用 `record_fills.py --order-day <订单日> --template` 生成模板。
3. **模型加载与工作目录解耦**：mlflow 实验记录存于 `research/mlruns`，默认按 cwd 解析会在
   `ops/run_daily` 子进程里找不到模型；`predict_daily` 显式指定 `exp_manager` uri 后任意目录可用。
4. **批量信号摊薄特征计算成本**：Alpha158 单日逐次推理约 4 分钟（重算 365 天特征），
   回填一周需多次；`predict_range.py` 把区间作为一个 test 段一次性推理再按日拆分，显著降本。
5. **试运行用模拟、转实盘前重置**：试运行期用 `mode: simulated` 攒数据；正式实盘前改回 `manual`
   并重置现金账本。`live_manual_10k` 已于 2026-06-22 完成切换（初始 12000，真实回填中）。
6. **研究模拟线仍是理想成交**：`simulate_fills` 已过滤停牌/开盘涨跌停不成交，但仍系统性略乐观；
   双线对比中的"成交偏差"即用于度量实盘相对研究线的真实滑点。
7. **evening 必须在 update_daily 后刷新交易日**：早期 `run_daily` 在入口锁定 `day`，update 把日历
   从旧日推进后仍用旧日出信号/清单（2026-06-15 晚间 research 误用 06-11 重跑）。已改为：未显式
   `--day` 时，数据更新成功后 `day ← latest_trading_day()` 再跑 predict/plan。
8. **postclose 的 order_day 链与断档补跑**：postclose(T) 默认读 `orders/prev(T)` 模拟 T 日开盘成交。
   跨周末例：周一 T=06-15 需周五 order_day=06-12 的清单；若 06-12 evening 未执行则 postclose CRIT
   （非 bug，是保护）。补法：`predict_range` 补信号 → `backfill --start <最后净值日> --end <最新日>`。

### 9.7 方案内增强补齐（2026-07，非主线变更）

> **结论：改动不大。** 未引入 Core-Satellite / ETF 混合，未改收益公式与分层契约；
> 只是把原文已写、§9.3 标为「待补齐」的四项落到代码，属于**原方案闭环补全**。

| 增强项 | 原文位置 | 落地文件 | 对现网影响 |
|---|---|---|---|
| 日亏硬熔断 | §3.5「停止开新仓」 | `common.set_halt` / `compute_nav` / `make_trade_plan` / `monitor` | 默认开启；只在日亏≥3% 时禁买 |
| 纸面跟踪晋升 | §3.3 关卡5 | `factor_lab/track_paper.py` | 默认只报告；`--promote` 才改 yaml |
| live 因子并入 | §3.3 / 流程图 | `research/alpha158_plus_lab.py` + workflow handler | **无 live 时 ≡ Alpha158**，旧模型可继续用 |
| 滚动重训 | §3.2 / §4 每月初 | `research/rolling_retrain.py` | 门禁未过不写生产 experiment |
| 行业偏离 ≤3% | §3.2 组合约束 | `execution/industry.py` + `build_industry_map.py` | 仅 `topk≥20` 启用；实盘 7 只不受影响 |

**明确未做（仍非本轮范围）**：A+B Hybrid / ETF 核心袖、仓位择时辅线、miniQMT 自动下单、
把未满纸面期的因子强行并入线上模型。

**调度**：`ops/crontab.example` 增加周日 `track_paper`、每月初 `rolling_retrain`
（与看板 APScheduler 二选一，勿重复）。

**纸面跟踪快照（2026-07-13，未 promote）**：`pv_corr` / `delta_pv_corr` 纸面 27 日，
近期 40 日 Rank IC 约 −0.025（与入库 OOS 同号）；满 40 日后再评估晋升。
报告：`data/reports/paper_track_20260713.md`。
